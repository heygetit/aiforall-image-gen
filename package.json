{
  "name": "aiforall-image-gen",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check plugins/aiforall-image-gen/scripts/generate.mjs",
    "check:setup": "node --check scripts/codex-setup.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:python": "python -m unittest discover -s tests -p 'test_*.py'"
  },
  "engines": {
    "node": ">=18"
  }
}
